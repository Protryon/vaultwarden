# Config installed into the global CONFIG when running the test suite.
# Embedded into the test binary via include_str! (see src/test_harness.rs), so
# it does not depend on the working directory. The `db` section here is only a
# placeholder to satisfy deserialization — the harness connects using the
# hardcoded constants in src/test_harness.rs and creates a fresh database per
# test, never using db::init().
api_bind: 127.0.0.1:10765
db:
  host: 127.0.0.1
  port: 5433
  username: vaultwarden
  password: vaultwarden
  database: postgres

data: testing/data
public: http://localhost:10765
# plaintext admin password is "test"
admin_token: $argon2id$v=19$m=16,t=2,p=1$dGVzdHRlc3Q$aME57ODlq+maIRv0ouzcLA
